### Example 1: Delete L3 network
```powershell
Remove-AzNetworkCloudL3Network -ResourceGroupName resourceGroupName -Name l3NetworkName -SubscriptionId subscriptionId
```

This command deletes the existing layer 3 (L3) network.
