### Example 1: Delete volume
```powershell
Remove-AzNetworkCloudVolume -Name volumeName -ResourceGroupName resourceGroupName -SubscriptionId subscriptionId
```

This command deletes an existing volume.
