### Example 1: Delete cluster manager
```powershell
Remove-AzNetworkCloudClusterManager -Name cmName -ResourceGroupName resourceGroupName -SubscriptionId subscriptionId
```

This command deletes the existing cluster manager.
