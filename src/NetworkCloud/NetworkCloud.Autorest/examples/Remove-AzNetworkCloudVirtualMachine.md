### Example 1: Delete virtual machine
```powershell
Remove-AzNetworkCloudVirtualMachine -Name vmName -ResourceGroupName resourceGroup -SubscriptionId subscriptionId
```

This command deletes the existing virtual machine.
