### Example 1: Stop bare metal machine
```powershell
Stop-AzNetworkCloudBareMetalMachine -Name bareMetalMachineName -ResourceGroupName resourceGroup -SubscriptionId subscriptionId
```

This command stops a bare metal machine.
