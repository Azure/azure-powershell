<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PsModuleName>CosmosDB</PsModuleName>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)..\..\Az.props" />
  <ItemGroup>
    <!-- Matches Az.KeyVault on this preview branch so both load one Keys assembly version. Preview-branch only; not for main. -->
    <PackageReference Include="Azure.Security.KeyVault.Keys" Version="4.11.0-beta.1" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\CosmosDB.Management.Sdk\CosmosDB.Management.Sdk.csproj" />
  </ItemGroup>

  <Import Project="$([MSBuild]::GetDirectoryNameOfFileAbove($(MSBuildThisFileDirectory).., build.proj))\src\Az.Post.props" />

</Project>
