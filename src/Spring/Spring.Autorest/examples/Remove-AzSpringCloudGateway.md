### Example 1: Disable the default Spring Cloud Gateway.
```powershell
Remove-AzSpringCloudGateway -ResourceGroupName azps_test_group_spring -ServiceName azps-spring-01 -Name default
```

Disable the default Spring Cloud Gateway.