<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PsModuleName>Billing</PsModuleName>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)..\..\Az.props" />

  <PropertyGroup>
    <AssemblyName>$(AzAssemblyPrefix)Consumption</AssemblyName>
    <RootNamespace>$(LegacyAssemblyPrefix)Consumption</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Consumption.Management.Sdk\Consumption.Management.Sdk.csproj" />
  </ItemGroup>

  <Import Project="$([MSBuild]::GetDirectoryNameOfFileAbove($(MSBuildThisFileDirectory).., build.proj))\src\Az.Post.props" />

</Project>
