### Example 1: Remove an Application Gateway for Containers resource
```powershell
Remove-AzAlb -Name test-alb -ResourceGroupName test-rg
```

This command removes an Application Gateway for Containers resource.
