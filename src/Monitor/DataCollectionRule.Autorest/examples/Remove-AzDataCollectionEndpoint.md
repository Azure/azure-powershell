### Example 1: Delete no association data collection endpoint
```powershell
Remove-AzDataCollectionEndpoint -Name myCollectionEndpoint -ResourceGroupName AMCS-TEST
```

This command deletes data collection endpoint with no existing association.

