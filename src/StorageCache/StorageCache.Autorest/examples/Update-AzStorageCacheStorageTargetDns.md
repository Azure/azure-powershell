### Example 1: Update storage target DNS
```powershell
Update-AzStorageCacheStorageTargetDns -CacheName "myCache" -ResourceGroupName "myResourceGroup" -StorageTargetName "myTarget"
```

The HPC Cache service is being deprecated.

