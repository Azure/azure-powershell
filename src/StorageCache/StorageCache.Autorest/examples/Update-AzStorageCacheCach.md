### Example 1: Update a cache
```powershell
Update-AzStorageCacheCach -CacheName "myCache" -ResourceGroupName "myResourceGroup" -Tag @{Environment="Production"}
```

The HPC Cache service is being deprecated.

