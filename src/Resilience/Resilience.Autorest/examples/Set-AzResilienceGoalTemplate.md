### Example 1: Replace a goal template
```powershell
Set-AzResilienceGoalTemplate -Name 'gt-tier1-resiliency' -ServiceGroupName 'azcmdlet-testing'
```

Replaces the specified goal template. Properties that are not supplied are reset.
