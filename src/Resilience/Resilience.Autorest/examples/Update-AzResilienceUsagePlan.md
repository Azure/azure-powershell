### Example 1: Update a usage plan
```powershell
Update-AzResilienceUsagePlan -Name 'up-payments' -ResourceGroupName 'rg-resiliency-prod'
```

Updates the specified usage plan, preserving properties that are not supplied.
