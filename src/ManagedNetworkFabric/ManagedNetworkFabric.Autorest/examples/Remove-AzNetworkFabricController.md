### Example 1: Delete the Network Fabric Controller Resource
```powershell
Remove-AzNetworkFabricController -Name $name -ResourceGroupName $resourceGroupName
```

This command deletes the Network Fabric Controller resource.

