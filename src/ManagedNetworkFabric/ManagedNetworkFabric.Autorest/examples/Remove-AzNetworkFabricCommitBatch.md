### Example 1: Delete the Commit Batch resource
```powershell
Remove-AzNetworkFabricCommitBatch -NetworkFabricName $networkFabricName -ResourceGroupName $resourceGroupName -CommitBatchId $commitBatchId
```

This command deletes the Commit Batch resource from the given Network Fabric.
