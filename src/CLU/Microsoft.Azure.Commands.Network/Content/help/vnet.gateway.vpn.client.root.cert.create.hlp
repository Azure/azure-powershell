az vnet gateway vpn client root cert create

Create a Vpn Client Root Certificate.

USAGE:

az vnet gateway vpn client root cert create --name <name> --publicCertData <publicCertData>

PARAMETERS:

--name
  [string] The name of the Client Root certificate

--publicCertData
  [string] Client Root Public certificate data

