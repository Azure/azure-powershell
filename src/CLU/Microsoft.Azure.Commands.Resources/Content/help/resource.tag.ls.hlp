az resource tag ls

Get Tag details.

USAGE:

az resource tag ls [[-n] <name>] [--detailed]

PARAMETERS:

--detailed
  [flag] Whether should get the tag values information as well.

-n (--name)
  [string] Name of the tag. If not specified, return all the tags of the subscription.

