<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="All" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
  </PropertyGroup>

  <Target Name="Build">
    <Exec Command="dotnet msbuild build/build.proj /p:Configuration=$(Configuration) /NoLogo" />
  </Target>

  <Target Name="Pack">
    <Exec Command="dotnet msbuild build/pack.proj /p:Configuration=$(Configuration) /NoLogo" />
  </Target>

  <Target Name="All" DependsOnTargets="Build;Pack" />
</Project>
