parameters:
  artifactName: ''

steps:
- task: PublishPipelineArtifact@0
  displayName: 'Save ${{ parameters.artifactName }}-${{ System.JobAttempt }}'
  inputs:
    artifactName: ${{ parameters.artifactName }}-${{ System.JobAttempt }}
    targetPath: artifacts
  condition: always()
